cinder-block-device:
  name: 'Block Device Driver'
  description: 'Block Device driver for Cinder'
  has_primary: false                # whether has primary role or not
  public_ip_required: false         # whether requires public net or not
  weight: 100
  limits:
    min: 1
  conflicts:
    - controller
    - cinder
